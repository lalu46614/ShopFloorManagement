{
  "name": "shop-floor-management",
  "version": "1.0.0",
  "description": "Shop Floor Management Module for Manufacturing AI Assistant",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "seed": "node database/seed.js",
    "prisma:generate": "prisma generate --schema=./database/schema.prisma",
    "prisma:migrate": "prisma migrate dev --name init --schema=./database/schema.prisma",
    "prisma:studio": "prisma studio --schema=./database/schema.prisma"
  },
  "keywords": [
    "manufacturing",
    "shop-floor",
    "machine-monitoring"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.7.1",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "prisma": "^5.7.1"
  }
}

